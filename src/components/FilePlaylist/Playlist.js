import React, { Component } from 'react';

class Playlist extends Component {
	render() {

		return (
			<div className="file-playlist">
				{/*<PlaylistHeader/>*/}
				{/*{undefined.map( (file, i) => {*/}

				{/*})}*/}
			</div>
		);
	}
}

export default Playlist;

const styles ={
	filename: {
		textOverflow: "ellipsis",
		overflow: "hidden",
		whiteSpace: "nowrap",
		color: "black"
	}
};